//! `dcs-protocol`: the shared, serializable contract surface between the
//! engine (`dcs-core`) and its clients (render / app).
//!
//! Defines `Command`, `GameEvent`, and `VersionedSave`. A stub for now;
//! concrete types arrive in later phases.
